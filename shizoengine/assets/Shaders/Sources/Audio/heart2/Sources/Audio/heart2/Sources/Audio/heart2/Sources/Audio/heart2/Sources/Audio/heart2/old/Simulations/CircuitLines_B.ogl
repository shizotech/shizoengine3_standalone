#alias iChannel0 A

void mainImage( out vec4 O, vec2 U ){
    O = texture(iChannel0, (U + vec2(.5))/iResolution.xy);
    // Cheap blur

    O = vec4(
        O.z*O.z,
        sqrt(O.z),
        O.z,
        1.
    );
}